<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title></title>
</head>
<body>
	
	<h1>LINGUAGEM PHP COM BANCO DE DADOS</h1>
	<p>// Sessão => Essencial para a solução de ajuste de preferencias de usuario, permitindo assim identificar ou visitantes atyraves de sessao id<br>
	// É uma forma simples de armazenar dados para usuarios individuais usando um id<br>
	// São enviados ao navegador atraves de cookies de sessao e o id usado serve para recuperar dados da sessao existente<br>
	//  A ausencia de um ID ou cookie permite que o PHP saiba que deve criar uma nova sessao e gerar um novo ID<br>	
	// Para iniciar uma função de sessão, use session_start(), assim voce permite as variaveis de sessao; 
	// Para usa-la, utilize a variavel superglobal $Session['']
	

	// É possivel eliminar todas as sessões atravez do session_destroy()
	// E para eliminar variaveis, mantendo as sessões é possivel usar o session_unset()

	// Para bloquear uma pagina por sessão existe um artificio;
 </p>

</body>
</html>